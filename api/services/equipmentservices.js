var Promise = require('bluebird'),
    promisify = Promise.promisify;

    module.exports = {

    saveEquip: function (data, context) {
    	/*return API.Model(Equipment).create({
            name: data.name,
            description: data.description,
        });*/
        console.log(data);
    },
   
};
